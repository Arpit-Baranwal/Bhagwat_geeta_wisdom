# Local development / debugging extras. Not needed in production images.
# Enable request tracing with PHOENIX_TRACING=1 (UI at http://localhost:6006).
-r requirements.txt
arize-phoenix>=4.0.0
openinference-instrumentation-anthropic>=0.1.0
