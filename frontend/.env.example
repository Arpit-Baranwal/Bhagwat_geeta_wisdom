# Base URL of the deployed FastAPI backend (no trailing slash).
# Local dev example: http://localhost:8000
# Production: your Render/Railway backend URL, e.g. https://geeta-wisdom-api.onrender.com
REACT_APP_BACKEND_URL=http://localhost:8000
